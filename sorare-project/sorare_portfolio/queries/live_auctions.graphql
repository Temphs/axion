query LiveAuctions($cursor: String) {
  tokens {
    liveAuctions(after: $cursor) {
      nodes {
        id
        currentPrice
        endDate
        minNextBid
        bestBid { amounts { wei eurCents } }
        anyCards {
          slug
          rarityTyped
          seasonYear
          inSeasonEligible
          anyPlayer { slug displayName }
        }
      }
      pageInfo { endCursor hasNextPage }
    }
  }
}
