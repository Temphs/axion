.env
data/
workbook/*.xlsx
!workbook/.gitkeep
.venv/
__pycache__/
*.pyc

# Sorare publishes this and the doctor downloads it on demand; it is 900 KB
# and changes monthly, so it does not belong in version control.
config/schema.graphql
